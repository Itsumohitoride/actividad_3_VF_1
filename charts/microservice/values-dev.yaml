# Environment-specific values: development
global:
  environment: development

replicaCount: 1

image:
  tag: d9d1efb
  pullPolicy: Always

service:
  type: NodePort

ingress:
  enabled: false

config:
  springProfilesActive: dev
