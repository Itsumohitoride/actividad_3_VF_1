# Environment-specific values: development
global:
  environment: development

replicaCount: 1

image:
  tag: 8d28d5f
  pullPolicy: Always

service:
  type: NodePort

ingress:
  enabled: false

config:
  springProfilesActive: dev
