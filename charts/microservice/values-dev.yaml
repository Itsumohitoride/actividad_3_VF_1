# Environment-specific values: development
global:
  environment: development

replicaCount: 1

image:
  tag: 0951a3c
  pullPolicy: Always

service:
  type: NodePort

ingress:
  enabled: false

config:
  springProfilesActive: dev
