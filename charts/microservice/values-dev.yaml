# Environment-specific values: development
global:
  environment: development

replicaCount: 1

image:
  tag: 07ab88c
  pullPolicy: Always

service:
  type: NodePort

ingress:
  enabled: false

config:
  springProfilesActive: dev
